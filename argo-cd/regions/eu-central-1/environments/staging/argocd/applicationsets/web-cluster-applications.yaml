apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: web-cluster-applications
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - matrix:
        generators:
          - clusters:
              selector:
                matchLabels:
                  web: "true"
          - git:
              repoURL: https://github.com/gatmbarz123/AWS-Project.git
              revision: main
              directories:
                - path: regions/eu-central-1/environments/staging/clusters/{{.name}}/values/apps/*
                - path: regions/eu-central-1/environments/staging/clusters/{{.name}}/values/apps/web-api
                  exclude: true
                - path: regions/eu-central-1/environments/staging/clusters/{{.name}}/values/apps/db-migrations-api
                  exclude: true
              
  template:      
    metadata:
      name: "{{.path.basename}}"
      namespace: argocd

    spec:
      # The project the application belongs to.
      project: default

      # Source of the application manifests
      sources:
      - repoURL: https://github.com/gatmbarz123/AWS-Project.git
        targetRevision: main
        ref: values

      - repoURL: https://github.com/gatmbarz123/AWS-Project.git
        targetRevision: main
        path: 'chart'
        helm:
          ignoreMissingValueFiles: true
          valueFiles:
            - $values/regions/eu-central-1/environments/staging/clusters/global-values.yaml
            - $values/regions/eu-central-1/environments/staging/clusters/{{.name}}/values/apps/{{.path.basename}}/values.yaml
      
      # Destination cluster and namespace to deploy the application
      destination:
        server: "{{.server}}"
        namespace: "stg"

      # Sync policy
      syncPolicy:
        automated: # automated sync by default retries failed attempts 5 times with following delays between attempts ( 5s, 10s, 20s, 40s, 80s ); retry controlled using `retry` field.
          prune: true # Specifies if resources should be pruned during auto-syncing ( false by default ).
          selfHeal: true
        syncOptions:
          - CreateNamespace=true

  templatePatch: |
    {{- if eq .path.basename "db-migrations" }}
    spec:
      syncPolicy:
        automated:
          selfHeal: false
    {{- end }}